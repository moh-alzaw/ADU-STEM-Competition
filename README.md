# ADU-STEM-Competition
Use the following command in the same directory as main.py to convert python file to exe: 

`python -m PyInstaller --noconfirm --onefile --windowed --add-data "web;web" main.py`
